namespace Binarysharp.MemoryManagement.Patterns
{
    /// <summary>
    /// Enum that defines basic pattern types. 
    /// </summary>
    public enum MemoryPatternType
    {
        /// <summary>
        /// A pattern that looks for a function pointer.
        /// </summary>
        Function,
        /// <summary>
        /// A pattern that looks for a data pointer.
        /// </summary>
        Data
    }
}